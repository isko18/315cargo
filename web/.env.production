# Прод-адрес API. Лежит в репозитории намеренно: локальный .env указывает на
# 127.0.0.1, и сборка без этого файла уезжала на прод с dev-адресом — панель
# при этом открывается, но все запросы падают с ERR_CONNECTION_REFUSED.
VITE_API_BASE=https://315cargo.com
